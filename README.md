# Hello World Web App 🌎

A simple HTML + JavaScript app served via Nginx and containerized with Docker.

## 🧱 Structure
- `index.html` — main webpage
- `app.js` — frontend logic
- `Dockerfile` — builds a container using Nginx

## 🚀 Build & Run

### Build Docker Image
```bash
docker build -t hello-world-app .
